- defaults:
    link:
      create: true
      relink: true
- shell:
    - [./deps, Installing deps]

- clean: ["~", "~/.config"]

- shell:
    - [git submodule update --init --recursive, Installing submodules]
